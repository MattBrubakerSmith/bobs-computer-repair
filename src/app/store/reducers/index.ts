import {
  ActionReducer,
  ActionReducerMap,
  createFeatureSelector,
  createSelector,
  MetaReducer
} from '@ngrx/store';

export interface State {
  // bobJobs: BobJob[];
  // firstName: string;
  // lastName: string;
  // email: string;
}

export const reducers: ActionReducerMap<State> = {
};
